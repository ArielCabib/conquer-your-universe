export interface ContextMenuState {
  canvasX: number;
  canvasY: number;
  offsetX: number;
  offsetY: number;
}

export interface InfoEntry {
  id: string;
  title: string;
  description: string;
}
